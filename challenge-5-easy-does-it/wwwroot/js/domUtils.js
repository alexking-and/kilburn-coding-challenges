window.focusBoard = () => document.getElementById("game-board").focus();

window.getBoardHeight = () => document.getElementById("game-board").getBoundingClientRect().height;

window.getBoardWidth = () => document.getElementById("game-board").getBoundingClientRect().width;

window.getWindowHeight = () => window.innerHeight;

window.getWindowWidth = () => window.innerWidth;
